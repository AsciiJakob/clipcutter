#include "pch.h"
#include "mediaClip.h"


void MediaClip_Init(MediaClip* mediaClip, MediaSource* mediaSource) {
	memset(mediaClip, 0, sizeof MediaClip);
	//mediaClip->source = mediaSource; // get back
	// savedTrackWidth = secToWidth(180)
	
}
